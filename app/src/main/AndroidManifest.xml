<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ApplicationInitiate"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PastelColors"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name="edu.northeastern.MrManage.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="edu.northeastern.MrManage.view.activity.ManageUserActivity" />
        <activity android:name="edu.northeastern.MrManage.view.activity.CustomerProfileActivity" />
        <activity android:name=".view.activity.ActiveOrderActivity" />
        <activity android:name=".view.activity.AddShipmentActivity"></activity>
        <activity android:name=".view.activity.DeliveryHistoryActivity" />
        <activity android:name=".view.activity.ReceivedOrderActivity" />
        <activity android:name=".view.activity.StocksHistoryActivity" />
    </application>

</manifest>